### passwd — simple user database for svnserve
### Plaintext by default; keep this file protected by filesystem perms.
### For stronger auth, prefer SASL or svn+ssh with system accounts.

[users]
# username = password
