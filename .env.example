# Copy this file to .env and adjust values as needed

# Comma- or newline-separated SSH public keys to authorize for the 'svn' user
# - Comma-separated works cross-shell: key1,key2
# - Or use literal \n for newlines (PowerShell-safe): key1\nkey2
# - Example keys below are placeholders; replace with your real public keys
SSH_AUTHORIZED_KEYS=
#SSH_AUTHORIZED_KEYS=ssh-ed25519 AAAAC3Nza... user1@example,key-type AAAAB3Nza... user2@example
#SSH_AUTHORIZED_KEYS=ssh-ed25519 AAAAC3Nza... user1@example\nssh-ed25519 AAAAB3Nza... user2@example

# Home directory for repositories inside the container
# Do not change unless you also adjust the docker volume mapping
HOME_DIR=/home/svn

#COMPOSE_PROJECT_NAME=svn
